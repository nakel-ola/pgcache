{
  "name": "pgcache",
  "version": "0.0.0",
  "private": true,
  "description": "PostgreSQL cache packages for Node.js and NestJS with a Redis-like API, TTL support, and UNLOGGED tables",
  "keywords": [
    "pgcache",
    "postgresql-cache",
    "postgres-cache",
    "postgresql",
    "postgres",
    "redis-alternative",
    "redis-like",
    "node-cache",
    "nestjs-cache",
    "caching",
    "ttl",
    "unlogged-tables"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nakel-ola/pgcache.git"
  },
  "homepage": "https://github.com/nakel-ola/pgcache#readme",
  "bugs": {
    "url": "https://github.com/nakel-ola/pgcache/issues"
  },
  "license": "MIT",
  "author": "",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm --filter \"./packages/**\" build",
    "test": "pnpm --filter \"./packages/**\" test",
    "test:watch": "pnpm --filter \"./packages/**\" test:watch",
    "lint": "oxlint .",
    "format": "oxlint --fix .",
    "format:check": "oxlint .",
    "typecheck": "pnpm --filter \"./packages/**\" typecheck",
    "clean": "pnpm --filter \"./packages/**\" clean && rm -rf node_modules",
    "dev": "pnpm --filter \"./packages/**\" dev",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "oxlint": "^0.18.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
